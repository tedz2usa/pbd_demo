<% if object && object.errors.size > 0 %>
  <div class='formErrorsExplanation'>
    <h2><%= pluralize(object.errors.size, "error") %> prohibited this record form being saved:</h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>